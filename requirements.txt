# model-monitor 런타임 의존성.
# 수집 로직(LiteLLM/k8s 조회)은 표준 라이브러리(urllib/ssl)만 쓰지만,
# 웹 서비스 계층은 FastAPI 스택을 사용한다.
#
# 배포 대상은 air-gapped 라서 재현 가능한 빌드가 필수다 -> 직접 의존성은 정확한
# 버전(==)으로 고정한다. transitive(uvicorn[standard] 의 uvloop/httptools/
# watchfiles 등) 까지 동결하려면 이미지 빌드(python:3.12)와 동일 환경에서
#   pip download -r requirements.txt -d wheels/   (또는 pip freeze -> constraints.txt)
# 로 휠을 벤더링한 뒤 `pip install --no-index --find-links wheels/` 로 설치한다.
fastapi==0.138.1
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
# 설정 파일을 .yaml 로 쓸 때만 필요(.json 은 표준 라이브러리로 동작)
PyYAML==6.0.3
