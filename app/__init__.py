"""model-monitor — LiteLLM → KServe → vLLM/SGLang 백엔드 모니터 (FastAPI).

__version__ 은 이 프로젝트의 단일 진실 공급원(single source of truth)이다.
Docker 이미지 태그(ci.sh/push.sh), /api/snapshot 의 version 필드, 대시보드 표기에
모두 이 값이 쓰인다. 버전을 올리려면 여기만 바꾼다.
"""

__version__ = "0.5.5"
